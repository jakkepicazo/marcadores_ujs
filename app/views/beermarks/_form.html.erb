<%= form_with(model: beermark, local: false) do |form| %>
  <% if beermark.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(beermark.errors.count, "error") %> prohibited this beermark from being saved:</h2>

      <ul>
        <% beermark.errors.each do |error| %>
          <li><%= error.full_message %></li>
        <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= form.label :name %>
    <%= form.text_field :name %>
  </div>

  <div class="field">
    <%= form.label :malts %>
    <%= form.text_field :malts %>
  </div>

  <div class="field">
    <%= form.label :alcohol %>
    <%= form.text_field :alcohol %>
  </div>

  <div class="field">
    <%= form.label :category_id %>
    <%= form.select :category_id, Category.pluck(:name, :id)%> 
  </div>

  <div class="field">
  <%= form.label :kind_id %>
  <%= form.select :kind_id, Kind.pluck(:name, :id) %> 
  </div>

  <div class="actions">
    <%= form.submit data: {confirm: 'Are you sure to create this beermark?' } %> 
  </div>
<% end %>
